# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

def basic_pods
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'KeychainSwift'
  pod 'TTTAttributedLabel'
end

target 'BabylonPost' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  basic_pods
end
